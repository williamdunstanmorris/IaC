apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bluejay.fullname" . }}-test-connection"
  labels:
    {{- include "bluejay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bluejay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
