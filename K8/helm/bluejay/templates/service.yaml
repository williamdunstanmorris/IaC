apiVersion: v1
kind: Service
metadata:
  name: {{ include "bluejay.fullname" . }}
  labels:
    {{- include "bluejay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bluejay.selectorLabels" . | nindent 4 }}
